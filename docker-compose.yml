services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - LIVEKIT_API_KEY=${KEY}
      - LIVEKIT_API_SECRET=${SECRET}
    restart: unless-stopped
